# TenX
#### A web dashboard to visualize election campaign data and display insights

## Instructions
The backend is built using FASTAPI(Python). Install the requirements first
```commandline
cd backend
pip install -r requirements.txt
```

Run the application
```commandline
python app.py
```