# TenX
#### A web dashboard to visualize election campaign data and display insights

## Instructions
The backend is built using Flask(Python). Install the requirements first
```commandline
cd backend
pip install -r requirements.txt
```

The server runs on port 5000. DB data is automatically seeded
```commandline
python app.py
```